// import { Sudoku } from "../typings"

// export const fetchPageInfo = async () => {
//     const res = await fetch(`${process.env.NEXT_PUBLIC_BASE_URL}/api/getSudoku`)

//     const data = await res.json()
//     const sudoku: Sudoku = data.sudoku

//     return sudoku
// }

// this is a design for a fetch function that will get the sudoku data from the db 

export {} // keeps it from throwing an error